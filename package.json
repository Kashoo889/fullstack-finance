{
  "name": "kashif-hisab-kitab-root",
  "version": "1.0.0",
  "description": "Root deployment for Hostinger",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd backend && npm install"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}